@inject NavigationManager nm;
@inject IJSRuntime js;
@inject EventHookGlobal eh;

<ListPage 
    PageTitle="Difficulties"
    PageNumber="PageNumber"
    PagePath="/Administration/Difficulties"
    From="[Game.Words.Difficulties]"
    VisibleRows='new List<string>() { "[DifficultyID]", "[Name]", "[ScoreMultiplier]", "[Enabled]", "[Default]" }'
    VisibleRowsTitles='new List<string>() { "DifficultyID", "Name", "Score Multiplier", "Enabled", "Default" }'
/>

@code {
    [Parameter]
    public int PageNumber { get; set; }
    [Parameter]
    public string DifficultyID { get; set; }
}