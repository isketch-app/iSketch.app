@if (SpaceEvenly)
{
    <div class="space-even">@ChildContent</div>
}
else
{
    <div>@ChildContent</div>
}

@code {
    [Parameter]
    public RenderFragment ChildContent { get; set; }
    [Parameter]
    public bool SpaceEvenly { get; set; } = false;
}