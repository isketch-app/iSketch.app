@inject Services.Database db;
@inject NavigationManager nm;
@inject IJSRuntime js;
@inject Services.EventHook eh;

<ListPage 
    PageTitle="Difficulties"
    PageNumber="PageNumber"
    SQLCon="db.Connection"
    PagePath="/Administration/Difficulties"
    TableName="[Words.Difficulties]"
    VisibleRows='new List<string>() { "[DifficultyID]", "[Name]", "[From]", "[To]" }'
    VisibleRowsTitles='new List<string>() { "DifficultyID", "Name", "Range Start", "Range End" }'
/>

@code {
    [Parameter]
    public int PageNumber { get; set; }
    [Parameter]
    public string DifficultyID { get; set; }
}