@inject Services.Database db;
@inject NavigationManager nm;
@inject IJSRuntime js;
@inject Services.EventHookGlobal eh;

<ListPage 
    PageTitle="Sessions"
    PageNumber="PageNumber"
    DB="db"
    PagePath="/Administration/Sessions"
    TableName="[Security.Sessions.Splice]"
    VisibleRows='new List<string>() { "[SessionID]", "[SessionTime]", "[SessionIP]", "[SessionIPVersion]", "[UserName]" }'
    VisibleRowsTitles='new List<string>() { "SessionID", "Session Time", "IP Address", "IP Version", "User Name" }'
/>

@code {
    [Parameter]
    public int PageNumber { get; set; }
    [Parameter]
    public string SessionID { get; set; }
}