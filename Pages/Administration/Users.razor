@inject Services.Database db;
@inject NavigationManager nm;
@inject IJSRuntime js;
@inject Services.EventHookGlobal eh; 

<ListPage 
    PageTitle="UserID"
    PageNumber="PageNumber"
    DB="db"
    PagePath="/Administration/Users"
    TableName="[Security.Users.Splice]"
    VisibleRows='new List<string>() { "[UserID]", "[UserName]", "[Email]", "[CreatedTime]", "[LastLogonTime]", "[IdpName]" }'
    VisibleRowsTitles='new List<string>() { "[UserID]", "User Name", "Email", "Created", "Last Logon", "IDP" }'
/>

@code {
    [Parameter]
    public int PageNumber { get; set; }
    [Parameter]
    public string UserID { get; set; }
}